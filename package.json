{
  "name": "scldp-frontend-server",
  "version": "1.0.0",
  "description": "Frontend server for SCLDP",
  "main": "frontend-server.js",
  "scripts": {
    "start": "node frontend-server.js",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=14"
  }
}