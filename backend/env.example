# =============================================================================
# CONFIGURACIÓN DEL SISTEMA LPDP - LEY 21.719
# =============================================================================

# Configuración de Base de Datos
DATABASE_URL=postgresql://usuario:password@localhost:5432/lpdp_master
DATABASE_URL_MASTER=postgresql://usuario:password@localhost:5432/lpdp_master

# Configuración de Seguridad
SECRET_KEY=tu_clave_secreta_muy_larga_y_compleja_aqui_2024
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Configuración de Encriptación
LICENSE_ENCRYPTION_KEY=tu_clave_de_encriptacion_32_caracteres_aqui_2024

# Configuración Multi-Tenant
TENANT_SCHEMA_PREFIX=tenant_
TENANT_DEFAULT_SCHEMA=public

# Configuración de Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_email@gmail.com
SMTP_PASSWORD=tu_password_de_aplicacion
EMAILS_FROM_EMAIL=no-reply@tusistema.com
EMAILS_FROM_NAME=Sistema LPDP

# Configuración de Usuario Administrador
ADMIN_EMAIL=admin@tusistema.com
ADMIN_PASSWORD=Admin123!

# Configuración de Precios de Módulos (CLP)
MODULE_PRICES={
    "consentimientos": 50000,
    "arcopol": 45000,
    "inventario": 60000,
    "brechas": 40000,
    "dpia": 80000,
    "transferencias": 35000,
    "auditoria": 70000,
    "capacitacion": 30000
}

# Configuración de Desarrollo
DEBUG=true
ENVIRONMENT=development
LOG_LEVEL=INFO

# Configuración de CORS
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:3001"]

# Configuración de API
API_V1_STR=/api/v1
PROJECT_NAME=Sistema LPDP
VERSION=1.0.0

# Configuración de Supabase (opcional)
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_KEY=tu_supabase_anon_key

# Configuración de Render (opcional)
RENDER_EXTERNAL_HOSTNAME=tu-app.onrender.com
